version: "3.8"

services:
  spider-panel:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./pb_data:/pb_data
    env_file:
      - .env
    ports:
      - "8090:8090"
    networks:
      - spider-vpn-network
    restart: "always"

networks:
  spider-vpn-network:
    external: true
