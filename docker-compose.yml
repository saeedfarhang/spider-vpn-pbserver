services:
  spider-vpn-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    volumes:
      - ./pb_data:/pb_data
    env_file:
      - .env
    ports:
      - ${APP_PORT}:${APP_PORT}
    networks:
      - spider-vpn-network
    restart: "always"

networks:
  spider-vpn-network:
    external: true
